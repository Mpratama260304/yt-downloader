# ============================================
# YouTube Downloader - Environment Variables
# ============================================
#
# ⚠️⚠️⚠️  WARNING: THIS FILE IS FOR REFERENCE ONLY! ⚠️⚠️⚠️
#
# This file documents available environment variables.
# It is NOT loaded automatically in Docker deployments!
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚨 PHALA CLOUD DEPLOYMENT NOTICE 🚨
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Phala Cloud does NOT support:
#   ❌ .env files
#   ❌ Building images from Dockerfile at deploy time
#
# You MUST:
#   ✅ Build image locally: docker build -t username/youtube-downloader:latest .
#   ✅ Push to Docker Hub: docker push username/youtube-downloader:latest
#   ✅ Use only 'image:' in docker-compose.yml (NO 'build:' section!)
#   ✅ Set env vars in docker-compose.yml environment section
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# For Phala Cloud, Railway, and other Docker-based platforms:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DO NOT create .env or .env.local files!
# These platforms ignore .env files completely.
#
# Set ALL environment variables directly in docker-compose.yml:
#
#   services:
#     app:
#       image: YOUR_USERNAME/youtube-downloader:latest  # Required!
#       environment:
#         - NODE_ENV=production
#         - PORT=3000
#         - HOSTNAME=0.0.0.0
#         - NEXT_PUBLIC_APP_URL=https://your-app-id.dstack.phala.network
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# For local development only, copy to .env.local:
#   cp .env.example .env.local
# ============================================

# ============================================
# REQUIRED IN PRODUCTION
# ============================================

# Application environment (development | production)
# Default: production (in Docker)
NODE_ENV=development

# Server port
# Default: 3000
PORT=3000

# Server hostname (use 0.0.0.0 for Docker)
# Default: 0.0.0.0
HOSTNAME=0.0.0.0

# Public application URL (for SEO, meta tags, etc.)
# Default: http://localhost:3000
# In production, set to your actual domain!
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Custom yt-dlp binary path (if not using default)
# Default: ./bin/yt-dlp or system yt-dlp
# YTDLP_PATH=/path/to/yt-dlp

# Rate limiting configuration
# Default: 10 requests per 60 seconds
# RATE_LIMIT=10
# RATE_WINDOW=60000

# Cookie expiry time in milliseconds
# Default: 3600000 (1 hour)
# COOKIE_EXPIRY=3600000

# Logging level (debug | info | warn | error)
# Default: info
# LOG_LEVEL=info

# ============================================
# DOCKER-SPECIFIC (set in docker-compose.yml)
# ============================================
# NEXT_TELEMETRY_DISABLED=1
# TZ=UTC

